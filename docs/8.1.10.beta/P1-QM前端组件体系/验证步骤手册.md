# QM 前端组件体系 — 验证步骤手册

## 环境准备

### 1. 启动 Docker 基础设施
```bash
cd foggy-dataset-demo/docker
docker compose up -d
```
确认以下服务运行中：
- MySQL: `localhost:13306` (foggy_test)
- MongoDB: `localhost:17017`

### 2. 打包并启动后端
```bash
# 打包
mvn package -pl foggy-mcp-launcher -am -DskipTests

# 启动（标准模式，含 data-viewer）
java -jar foggy-mcp-launcher/target/foggy-mcp-launcher-8.1.10.beta.jar \
  "--spring.data.mongodb.uri=mongodb://localhost:17017/foggy_viewer" \
  "--spring.datasource.url=jdbc:mysql://localhost:13306/foggy_test?useSSL=false&allowPublicKeyRetrieval=true" \
  --spring.datasource.username=foggy \
  --spring.datasource.password=foggy_test_123 \
  --server.port=7108
```

等待 `Started McpLauncherApplication` 日志输出。

---

## 验证步骤

### 步骤 1：frontend-meta v1 接口

**请求**
```bash
curl -s http://localhost:7108/data-viewer/api/frontend-meta/FactOrderQueryModel | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `code` | `200` |
| `data.metaVersion` | `"v1"` |
| `data.model` | `"FactOrderQueryModel"` |
| `data.caption` | `"订单查询"` |
| `data.fields` | 数组（非对象），约 40 个字段 |
| `data.fields[0].category` | `"dimension-id"` (orderDate$id) |
| `data.fields[1].category` | `"dimension-caption"` (orderDate$caption) |
| `data.fields[1].memberLookup.enabled` | `true` |
| `data.fields[1].memberLookup.selectionFieldName` | `"orderDate$id"` |
| `dimension-id` 字段的 `uiHints.visible` | `false` |
| 度量字段的 `sortable` | `false` |
| 度量字段的 `aggregatable` | `true` |
| 字典字段（orderStatus）有 `dictId` | `"order_status"` |
| 字典字段有 `dictMode` | `"static"` |
| 无 `prompt`、`meta`、`models` 嵌套 | ✅ |

### 步骤 2：维度成员查询 — 基本加载

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{
    "qmModel": "FactOrderQueryModel",
    "fieldName": "customer$caption",
    "start": 0,
    "limit": 5
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.selectionFieldName` | `"customer$id"` |
| `data.displayFieldName` | `"customer$caption"` |
| `data.items` | 5 条，每条有 `value` + `label` |
| `data.total` | > 0（demo 数据约 1000） |
| `data.hasMore` | `true` |

### 步骤 3：维度成员查询 — keyword 搜索

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{
    "qmModel": "FactOrderQueryModel",
    "fieldName": "store$caption",
    "keyword": "杭州",
    "start": 0,
    "limit": 5
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.items[].label` | 每条都包含"杭州" |
| `data.total` | 应小于全量门店数 |
| `data.selectionFieldName` | `"store$id"` |

### 步骤 4：维度成员查询 — selectedValues 回填

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{
    "qmModel": "FactOrderQueryModel",
    "fieldName": "store$caption",
    "keyword": "",
    "start": 0,
    "limit": 3,
    "selectedValues": [1, 3]
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.items` | 3 条（正常查询结果） |
| `data.selectedItems` | 2 条（id=1 和 id=3 的门店） |
| `data.selectedItems[].value` | `1` 和 `3` |
| `data.selectedItems[].label` | 对应门店名称 |

### 步骤 5：维度成员查询 — 层级查询 (childrenOf)

> 注意：`FactOrderQueryModel` 没有层级维度，层级验证使用 `FactTeamSalesQueryModel`。

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{
    "qmModel": "FactTeamSalesQueryModel",
    "fieldName": "team$caption",
    "start": 0,
    "limit": 10,
    "hierarchy": {
      "op": "childrenOf",
      "value": "T001"
    }
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.selectionFieldName` | `"team$id"` |
| `data.items` | 返回 T001 的子节点 |
| 子节点的 `value` | 不等于 `"T001"`（不含自身） |

> 若 `FactTeamSalesQueryModel` 不可用，跳过此步，在备注中标记。

### 步骤 6：直连查询 — 基础分页

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/query/direct/FactOrderQueryModel \
  -H "Content-Type: application/json" \
  -d '{
    "start": 0,
    "limit": 3,
    "orderBy": [{"field": "orderTime", "dir": "DESC"}]
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.success` | `true` |
| `data.items` | 3 条订单数据 |
| `data.total` | > 0 |
| `data.hasMore` | `true` |
| 首条 `orderTime` | 应为数据集中最新的时间 |
| 每条数据包含维度字段 | `customer$caption`, `store$caption` 等 |
| 每条数据包含度量字段 | `amount`, `payAmount` 等 |

### 步骤 7：直连查询 — 多条件筛选

**请求**
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/query/direct/FactOrderQueryModel \
  -H "Content-Type: application/json" \
  -d '{
    "start": 0,
    "limit": 2,
    "slice": [
      {"field": "orderStatus", "op": "=", "value": "PAID"},
      {"field": "customer$id", "op": "in", "value": [1, 2, 3]}
    ],
    "orderBy": [{"field": "amount", "dir": "DESC"}]
  }' | python -m json.tool
```

**验证要点**

| 检查项 | 预期 |
|--------|------|
| `data.items[].orderStatus` | 全部为 `"PAID"` |
| `data.items[].customer$id` | 只有 1、2、3 |
| 按 amount 降序 | 第一条 amount >= 第二条 |
| `data.totalData` | 包含汇总信息 |

### 步骤 8：DataViewer 表格渲染

**步骤**

1. 创建查询获取 queryId：
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/query/create \
  -H "Content-Type: application/json" \
  -d '{
    "model": "FactOrderQueryModel",
    "title": "订单查询验证",
    "payload": {
      "columns": ["orderId", "orderStatus", "customer$caption", "orderTime", "amount", "payAmount"],
      "slice": [{"field": "orderStatus", "op": "=", "value": "PAID"}],
      "orderBy": [{"field": "orderTime", "dir": "DESC"}]
    }
  }'
```
记下返回的 `queryId`。

2. 浏览器打开 `http://localhost:7108/data-viewer/view/FactOrderQueryModel/{queryId}`

**验证要点**

| 检查项 | 预期 |
|--------|------|
| 页面标题 | "订单查询验证" |
| 显示列 | 订单ID、订单状态、客户(名称)、下单时间、订单总额、订单应付金额 |
| 订单状态列 | 筛选框显示 "PAID" |
| 客户列 | 显示 SelectFilter 下拉框 |
| 合计行 | 显示总条数和度量合计 |
| 首行下单时间 | 应为数据集中最新时间（验证排序 `dir: DESC` 生效） |
| 分页器 | 存在且可切换页码（若不可见，记录为 **FAIL** 并检查控制台 `vxe-pager` 注册情况） |
| 浏览器控制台 | 无 4xx/5xx 网络错误（已知例外：`filter-options` 404 属旧接口兼容问题，记录但不阻塞） |

### 步骤 9：frontend-meta 下载

**请求**
```bash
curl -sI http://localhost:7108/data-viewer/api/frontend-meta/download/FactOrderQueryModel
```

**验证要点（响应头）**

| 检查项 | 预期 |
|--------|------|
| HTTP 状态码 | `200` |
| `Content-Type` | `application/json; charset=UTF-8` |
| `Content-Disposition` | 包含 `FactOrderQueryModel.frontend-meta.json` |

**请求（下载内容）**
```bash
curl -s -o FactOrderQueryModel.frontend-meta.json \
  http://localhost:7108/data-viewer/api/frontend-meta/download/FactOrderQueryModel
python -m json.tool FactOrderQueryModel.frontend-meta.json > /dev/null && echo "JSON valid"
```

| 检查项 | 预期 |
|--------|------|
| 文件保存成功 | `FactOrderQueryModel.frontend-meta.json` 存在 |
| JSON 格式正确 | `python -m json.tool` 无报错 |
| `metaVersion` | `"v1"` |

### 步骤 10：异常路径验证

#### 10a. qmModel 为空
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{"fieldName": "customer$caption"}' | python -m json.tool
```

| 检查项 | 预期 |
|--------|------|
| `code` | 非 200（如 600） |
| `msg` | 包含 "qmModel 不能为空" |

#### 10b. fieldName 为空
```bash
curl -s -X POST http://localhost:7108/data-viewer/api/members/query \
  -H "Content-Type: application/json" \
  -d '{"qmModel": "FactOrderQueryModel"}' | python -m json.tool
```

| 检查项 | 预期 |
|--------|------|
| `code` | 非 200 |
| `msg` | 包含 "fieldName 不能为空" |

#### 10c. 不存在的模型
```bash
curl -s http://localhost:7108/data-viewer/api/frontend-meta/NonExistentModel | python -m json.tool
```

| 检查项 | 预期 |
|--------|------|
| 不返回 500 | 应为 404 或带错误消息的响应 |

---

## Playwright 自动化验证结果

以下为 2026-04-04 使用 Playwright 实际验证的截图和输出：

### API 验证

| 接口 | 状态 | 关键验证点 |
|------|------|-----------|
| `GET /frontend-meta/FactOrderQueryModel` | ✅ 200 | metaVersion=v1, 40 字段, 5 个 memberLookup 自动推导 |
| `POST /members/query` (基础) | ✅ 200 | selectionFieldName=store$id, 50 条, hasMore=true |
| `POST /members/query` (keyword=杭州) | ✅ 200 | 10 条杭州门店, 过滤正确 |
| `POST /members/query` (selectedValues) | ✅ 200 | items=3 + selectedItems=2, 回填正确 |
| `POST /query/direct` (多条件) | ✅ 200 | PAID+customer∈[1,2,3], 8 条, totalData 汇总正确 |
| `POST /query/create` → DataViewer | ✅ 渲染 | 表格 6 列, 1951 条, 合计行, 筛选器正常 |

### 截图

| 截图 | 说明 |
|------|------|
| [03-data-viewer-table.png](screenshots/03-data-viewer-table.png) | Playwright 首次验证 |
| [04-data-viewer-table-rerun.png](screenshots/04-data-viewer-table-rerun.png) | 复核验证（排序参数修正后） |

---

## 已知限制与待跟进项

| 编号 | 问题 | 状态 | 说明 |
|------|------|------|------|
| K1 | `filter-options` 404 | 已知 | DataViewer queryId 模式下 SearchToolbar 走旧接口。新的 `GeneratedTableExample` 链路使用 `filterMemberLoader` 不受影响 |
| K2 | `vxe-pager`/`vxe-tooltip` 未注册 | 已知 | 打包模式下 vxe-table 全量注册问题，不影响核心查询功能，需后续修复 |
| K3 | 层级响应元数据不完整 | 待跟进 | `MemberQueryService.hierarchyEnabled` 未从模型元数据推导，树形选择体验待完善 |
| K4 | `GeneratedTableExample.vue` 需 dev 服务 | 已知 | 需 `npm run dev` 启动前端 dev 服务才能在浏览器运行（App.vue 无路由到 example） |
| K5 | `QueryPanel` 需 dev 联调 | 已知 | DataViewer queryId 页面走旧模式，QueryPanel 在直连模式下生效 |

## 修订历史

| 日期 | 修订 |
|------|------|
| 2026-04-04 | 初版 |
| 2026-04-04 | 根据复核报告修订：修正排序参数 `order`→`dir`、新增层级验证(Step 5)、新增异常路径验证(Step 10)、补充下载响应头校验(Step 9)、DataViewer 页面增加控制台/分页器检查点(Step 8)、增加已知限制清单 |
