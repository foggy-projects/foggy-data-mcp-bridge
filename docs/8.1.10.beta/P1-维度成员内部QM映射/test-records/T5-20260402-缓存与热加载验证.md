# T5 测试记录：缓存与热加载验证

## 基本信息
- 日期：`2026-04-02`
- 阶段：`阶段5：缓存、热加载与文档收口`
- 测试人：`Codex`

## 测试范围
- synthetic member-QM namespace 级缓存复用
- `clearByNamespace` 失效与重建
- `BundleRemovedEvent` 失效与重建
- `FsscriptRemoveEvent` 失效与重建
- MVP 样例文档收口检查

## 测试命令
- 生命周期与运行时回归：
```powershell
mvn -pl foggy-dataset-model -P !multi-db "-Dtest=SyntheticMemberQueryModelLifecycleTest,SyntheticMemberQueryModelRuntimeTest,SyntheticMemberQueryModelResolverTest" test
```

- 最终覆盖率报告：
```powershell
mvn -pl foggy-dataset-model -P !multi-db org.jacoco:jacoco-maven-plugin:0.8.12:prepare-agent "-Dtest=SyntheticMemberQueryModelResolverTest,SyntheticMemberQueryModelRuntimeTest,SyntheticMemberQueryModelLifecycleTest" test org.jacoco:jacoco-maven-plugin:0.8.12:report
```

## 关键用例
- `syntheticMemberQueryModelIsCachedWithinNamespace`
- `clearByNamespaceInvalidatesSyntheticMemberQueryModel`
- `bundleRemovedEventInvalidatesSyntheticMemberQueryModel`
- `fileRemoveEventInvalidatesSyntheticMemberQueryModel`

## 关键执行结果
- `SyntheticMemberQueryModelLifecycleTest + SyntheticMemberQueryModelRuntimeTest + SyntheticMemberQueryModelResolverTest`：
```text
Tests run: 24, Failures: 0, Errors: 0, Skipped: 0
```

## 覆盖率结果
- 报告位置：
  - `foggy-dataset-model/target/site/jacoco/index.html`
  - `foggy-dataset-model/target/site/jacoco/jacoco.csv`
- 关键类覆盖率：
  - `QueryModelLoaderImpl`：行覆盖 `74.0%`，分支覆盖 `58.0%`
  - `SyntheticMemberQueryModelResolver`：行覆盖 `90.9%`，分支覆盖 `71.8%`
  - `SyntheticMemberQueryModelFactory`：行覆盖 `90.0%`，分支覆盖 `62.9%`
  - `SyntheticMemberExternalPatchStep`：行覆盖 `85.8%`，分支覆盖 `67.7%`
  - `JdbcServiceImpl`：行覆盖 `66.2%`，分支覆盖 `45.5%`

## 文档检查
- 已提供普通维度 simple 入口样例：通过
- 已提供嵌套维度 DSL 样例：通过
- 已提供父子维 hierarchy 样例：通过
- 已提供 external patch 样例：通过
- 已明确当前 MVP 边界：通过

## 验收对照
- synthetic member-QM 在同一 namespace 下可复用缓存：通过
- `clearByNamespace` 后 synthetic member-QM 可重新装载：通过
- `BundleRemovedEvent` 后 synthetic member-QM 可重新装载：通过
- `FsscriptRemoveEvent` 后 synthetic member-QM 可重新装载：通过
- 文档明确 simple、DSL、external patch 的使用方式：通过

## 结论
- Stage5 验收通过，阶段 1-5 已全部完成。
