# T2 测试记录：主链接入与运行时 SQL 验证

## 基本信息
- 日期：`2026-04-02`
- 阶段：`阶段2：统一接入 QueryFacade 主链`
- 测试人：`Codex`

## 测试范围
- synthetic member-QM 运行时装载
- simple 入口归一到 QueryFacade 主链
- synthetic member-QM 字段边界控制
- synthetic model 直查 SQL 生成

## 测试数据
- 使用 `foggy-dataset-model` 模块自带 `sqlite` 测试数据
- 目标模型：
  - `FactSalesNestedDimQueryModel`
  - synthetic model：`FactSalesNestedDimQueryModel#product`

## 测试命令
- 阶段1解析回归：
```powershell
mvn -pl foggy-dataset-model -Dtest=SyntheticMemberQueryModelResolverTest test
```
- 阶段2运行时主链验证：
```powershell
mvn -pl foggy-dataset-model -P !multi-db -Dtest=SyntheticMemberQueryModelRuntimeTest test
```
- 阶段2临时覆盖率报告：
```powershell
mvn -pl foggy-dataset-model -P !multi-db org.jacoco:jacoco-maven-plugin:0.8.12:prepare-agent "-Dtest=SyntheticMemberQueryModelResolverTest,SyntheticMemberQueryModelRuntimeTest" test org.jacoco:jacoco-maven-plugin:0.8.12:report
```

## 覆盖场景
- 普通维度：
  - root `id/caption` 可查询
  - root 属性可查询
- 内嵌子维度：
  - `productCategory$caption`
  - `productCategory$categoryGroup$groupType`
- 父子维：
  - 本阶段未作为主验收场景执行运行时查询，留待阶段3
- 外部权限 patch：
  - 本阶段未实现，留待阶段4

## 输入样例
- synthetic model 直查：
```json
{
  "queryModel": "FactSalesNestedDimQueryModel#product",
  "columns": ["id", "caption", "productCategory$caption"]
}
```

- simple 入口归一前输入：
```json
{
  "queryModel": "FactSalesNestedDimQueryModel",
  "dimension": "categoryGroup$groupType",
  "start": 0,
  "limit": 20
}
```

## 关键执行现象
- loader 成功运行时装载 synthetic model：
```text
已为模型 FactSalesNestedDimQueryModel#product 分配简称: FSNDQM
```

- factory 成功生成运行时 schema/columns：
```text
schemaFields=[id, caption, productId, brand, unitPrice, productCategory$id, ...]
queryColumns=[id, caption, productId, brand, unitPrice, productCategory$id, ...]
```

- synthetic model 直查生成 SQL：
```sql
select d2.product_key "id",
       d2.product_name "caption",
       d3.category_name "productCategory$caption"
from dim_product_nested d2
left join dim_category_nested d3 on d2.category_key=d3.category_key
```

- simple 入口归一后进入同一主链：
```text
QueryFacade.beforeQuery completed, queryType=NORMAL, model=FactSalesNestedDimQueryModel#product
```

## 覆盖率结果
- 报告位置：
  - `foggy-dataset-model/target/site/jacoco/index.html`
  - `foggy-dataset-model/target/site/jacoco/jacoco.csv`
- 关键类覆盖率：
  - `SyntheticMemberQueryModelResolver`：行覆盖 `90.9%`，分支覆盖 `70.9%`
  - `SyntheticMemberQueryModelFactory`：行覆盖 `87.0%`，分支覆盖 `60.0%`
  - `JdbcServiceImpl`：行覆盖 `69.7%`，分支覆盖 `62.5%`
  - `QueryModelLoaderImpl`：整体类行覆盖 `71.4%`，分支覆盖 `57.1%`

## 预期结果
- synthetic member-QM 可被 loader 装载
- synthetic model 可直接生成 SQL
- simple 入口归一后进入 QueryFacade 主链
- synthetic model 不暴露 `salesAmount`、`store$caption` 等根维度子树外字段

## 实际结果
- 上述场景全部通过
- Stage2 新主路径已可工作
- `-P !multi-db` 下测试稳定
- 默认 `multi-db` profile 配合 `-Dtest=...` 会触发第二轮 surefire `ClassNotFoundException`，已识别为阶段测试命令问题，不影响本阶段功能结论

## 结论
- Stage2 验收通过，可进入阶段3。
