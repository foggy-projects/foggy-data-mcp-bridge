# T3 测试记录：嵌套子维与父子维运行时验证

## 基本信息
- 日期：`2026-04-02`
- 阶段：`阶段3：嵌套子维度与父子维验证`
- 测试人：`Codex`

## 测试范围
- 根维度 canonical 字段查询
- 一级/二级嵌套子维路径字段查询
- 子维路径字段过滤与排序
- 父子维 hierarchy operator
- simple 入口在父子维字段上的 synthetic 归一

## 测试命令
- Stage3 运行时测试：
```powershell
mvn -pl foggy-dataset-model -P !multi-db -Dtest=SyntheticMemberQueryModelRuntimeTest test
```

- Stage3 覆盖率报告：
```powershell
mvn -pl foggy-dataset-model -P !multi-db org.jacoco:jacoco-maven-plugin:0.8.12:prepare-agent "-Dtest=SyntheticMemberQueryModelResolverTest,SyntheticMemberQueryModelRuntimeTest" test org.jacoco:jacoco-maven-plugin:0.8.12:report
```

## 关键用例
- `querySyntheticMemberWithRootAndNestedPaths`
- `querySyntheticMemberWithNestedDimensionFilterAndOrder`
- `querySyntheticMemberWithDescendantsOf`
- `querySyntheticMemberWithAncestorsOf`
- `querySyntheticMemberWithSelfAndAncestorsOf`
- `querySyntheticMemberWithChildrenOf`
- `queryDimensionDataViaSyntheticModelForParentChildDimension`

## 测试结果
- `SyntheticMemberQueryModelRuntimeTest`：
```text
Tests run: 12, Failures: 0, Errors: 0, Skipped: 0
```

- `SyntheticMemberQueryModelResolverTest + SyntheticMemberQueryModelRuntimeTest`：
```text
Tests run: 16, Failures: 0, Errors: 0, Skipped: 0
```

## 覆盖率结果
- 报告位置：
  - `foggy-dataset-model/target/site/jacoco/index.html`
  - `foggy-dataset-model/target/site/jacoco/jacoco.csv`
- 关键类覆盖率：
  - `SyntheticMemberQueryModelResolver`：行覆盖 `90.9%`，分支覆盖 `70.9%`
  - `SyntheticMemberQueryModelFactory`：行覆盖 `90.0%`，分支覆盖 `62.9%`
  - `JdbcServiceImpl`：行覆盖 `65.6%`，分支覆盖 `45.5%`
  - `QueryModelLoaderImpl`：行覆盖 `71.4%`，分支覆盖 `57.1%`
  - `QueryRequestValidationStep`：行覆盖 `53.5%`，分支覆盖 `37.2%`

## 验收对照
- 可查询根维度属性：通过
- 可查询一级、二级内嵌子维度的 `id/caption/属性`：通过
- 可在子维度路径字段上执行过滤与排序：通过
- 父子维 `childrenOf / descendantsOf / ancestorsOf / selfAndDescendantsOf / selfAndAncestorsOf` 可工作：通过
- simple 入口可归一到同一 synthetic member-QM：通过

## 结论
- Stage3 验收通过，可进入 Stage4。
