# 视频1：《AI数据分析师来了！让企业安全用上AI分析》

## 视频基本信息
- **时长**: 3分钟
- **主题**: Trae CN集成演示
- **目标**: 展示Foggy MCP Bridge如何让企业安全地使用AI查询能力

## 视频脚本（分镜设计）

### 开场（0-30秒）
**画面**: AI助手快速生成SQL查询的动画效果，展示AI的强大能力
**语音**: "大家好！现在的AI助手越来越智能，已经有相当多成熟的text-to-SQL的mcp工具。但在企业项目中，我想应该没有哪位勇士会直接给客户提供text-to-SQL的MCP工具吧？"

**画面**: 展示企业级应用的安全顾虑场景
```sql
-- AI生成的SQL虽然强大，但...
SELECT * FROM sys_user WHERE user_id = '123';
-- 谁能保证不会意外泄露敏感信息？
```
**语音**: "如果 AI 为了回答一个统计问题，顺便把数据库的权限体系甚至用户隐私给‘扒’了出来，你的年终奖还安全不?"

### 解决方案介绍（30-90秒）
**画面**: 切换到Foggy MCP Bridge架构图，突出安全控制层
**语音**: "Foggy Data MCP Bridge提供了一套开源解决方案，它通过语义层模型，给 AI 加了一个**‘带权限的菜单’**，AI 只能点菜单上的菜，而不能直接进后厨乱翻。"

**画面**: 展示DSL查询示例，强调安全控制
```json
{
  "model": "SalesQueryModel",  // 预定义的语义层模型
  "payload": {
    "filters": [{"field": "date", "operator": ">=", "value": "2024-01-01"}]
  }
}
-- 只能访问预定义的字段，无法越权查询
```

### Trae CN集成演示（40-60秒）
**画面**: 打开Trae CN，进入MCP配置界面
**语音**: "现在我们来快速集成到Trae CN。只需要三步："

1. **启动服务**（5秒）
   - 演示Docker一键启动
   - 展示服务状态检查

2. **配置MCP服务**（5秒）
   - 打开设置 → MCP → 原始配置
   - 添加Foggy MCP服务地址

3. **自然语言查询**（30秒）
   - 在Builder with MCP智能体中输入查询
   - 展示实时查询结果

### 效果验证（150-180秒）
**画面**: 企业级应用场景演示
**语音**: "让我们看看企业级应用的实际效果："

- **安全控制**: 展示字段级权限如何生效
- **效率提升**: 业务人员直接使用自然语言查询
- **可视化**: 自动生成专业图表，无需额外开发

**画面**: 展示最终查询结果和图表，突出企业价值
**语音**: "就这样，3分钟时间，我们为企业打造了一个既安全又高效的AI数据分析平台！业务人员可以直接用自然语言查询，开发人员也不用担心数据安全问题。您接下来只需要为您的数据库建立语义层即可"

## 技术要点展示

### 关键配置代码
```json
{
  "mcpServers": {
    "foggy-dataset": {
      "url": "http://localhost:7108/mcp/analyst/rpc"
    }
  }
}
```

### 自然语言查询示例
```
查询最近一周的销售数据，按产品分类汇总，并生成柱状图
```

### 安全特性强调
- ✅ DSL查询消除SQL注入风险
- ✅ 字段级权限控制
- ✅ 只读设计确保数据安全

## 制作注意事项

### 视觉设计
- **主色调**: 科技蓝 + 安全绿
- **字体**: 等宽字体展示代码
- **动画**: 使用简洁的过渡动画

### 语音要求
- **语速**: 适中偏快，符合技术视频节奏
- **语调**: 专业但不生硬，带有适当的兴奋感
- **技术术语**: 清晰准确，避免模糊

### 屏幕录制要点
- **分辨率**: 1920x1080
- **鼠标轨迹**: 清晰可见
- **操作速度**: 适中，便于观众跟随

## 后续行动引导

**结尾画面**: GitHub项目链接 + 二维码
**语音**: "想了解更多？访问GitHub项目主页，查看完整文档和示例代码。下期我们将深入讲解Java集成，敬请期待！"

---

## AI语音生成建议

### 语音参数
- **语音类型**: 中文女声，专业技术风格
- **语速**: 1.2倍正常语速
- **音调**: 中等偏高，保持活力

### 文本分段
将脚本按30秒为单位分段，便于语音生成和后期剪辑。

### 技术术语发音
确保以下术语发音准确：
- MCP (Model Context Protocol)
- DSL (Domain Specific Language)
- Trae CN (字节跳动Trae)
- SQL注入 (SQL Injection)