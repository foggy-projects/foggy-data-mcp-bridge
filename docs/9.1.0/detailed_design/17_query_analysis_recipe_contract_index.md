---
doc_role: detailed_design
doc_purpose: Provide a signed and unsigned recipe index for stable non-SLA query and analysis engine capabilities.
version: 9.1.0
target: Query analysis recipe contract index
status: active-contract
created_at: 2026-06-02
updated_at: 2026-06-02
---

# Query Analysis Recipe Contract Index

## Purpose

This index records the stable non-SLA query and analysis recipes that planner, prompt, evaluator, and runtime owners can treat as signed engine contracts.

This document makes the existing `query_model`, `timeWindow`, Pivot, DSL_CTE bridge, and result-stage formula boundaries explicit so future prompt/schema edits do not imply unsupported capabilities.

## Contract Matrix

| Recipe id | Status | Trigger | Engine behavior | Evidence |
|---|---|---|---|---|
| `query_model.detail_filter_sort_paginate` | signed | Detail rows with model fields, `slice`, `orderBy`, and `limit` | Runs through model-backed query lifecycle and permission checks; `orderBy` must use exposed fields or selected aliases. | QueryModel schema descriptions, regression runner |
| `query_model.grouped_aggregate_table` | signed | `columns` with grouped dimensions and aggregate aliases such as `sum(amountTotal)` | Produces grouped tables with explicit aliases; post-aggregate threshold semantics must use the documented aggregate path, not guessed SQL. | promoted semantic gate, `BUG-post-aggregate-alias-calculate-20260531.md` |
| `query_model.grouped_topn_order_limit` | signed | Grouped aggregate TopN using selected aggregate aliases or measures in `orderBy` plus top-level `limit` | Applies top-level `orderBy` / `limit` after ordinary `query_model` grouped aggregation; this is not a Pivot axis TopN substitute. | `OdooModelLoadingTest#testSaleOrderGroupedTopNOrderLimitMatchesFixtureBaseline`, promoted semantic gate |
| `query_model.date_grain_self_dimension` | signed | Ordinary monthly/weekly/yearly grouping using described fields such as `salesDate$month` | Uses fields returned by `dataset.describe_model_internal`; does not synthesize SQL functions or hidden `$year` / `$month` fields. | QueryModel schema descriptions, semantic promoted samples, `OdooInvalidFieldRecoveryTest#testSynthesizedDateOrderQuarterIsRejectedBeforeSql` |
| `query_model.time_window_standard` | signed | YoY, MoM, WoW, YTD, MTD, rolling 7/30/90 day | Uses declarative `timeWindow`; `value` is optional but exactly two elements when provided; `rollingAggregator` supports `sum`, `avg`, `count`, `min`, and `max`. | `PivotSchemaValidationTest`, `QueryModelDescriptionConsistencyTest` |
| `query_model.time_window_post_calc_projection` | signed-constrained | `timeWindow` requests with `calculatedFields` that derive scalar expressions from timeWindow output columns | Applies a single outer projection after timeWindow expansion. Expressions may reference timeWindow output columns such as `salesAmount__ratio`; aggregate, window, `CALCULATE`, unknown columns, and sibling calculated-field alias dependencies are rejected before SQL preview. | `TimeWindowExecutionIntegrationTest#timeWindowPostCalcCalculateRejectedBySemanticPipeline`, `TimeWindowExecutionIntegrationTest#timeWindowPostCalcDependencyRejectedBySemanticPipeline`, `TimeWindowValidatorTest` |
| `query_model.time_window_final_order_limit` | signed | `timeWindow` requests with final `orderBy`, `start`, or `limit` over timeWindow output columns | Applies final sorting and pagination as an outer derived result stage after timeWindow expansion; output aliases such as `salesAmount__rolling_7d`, `salesAmount__prior`, and ordinary selected columns may be ordered. | `TimeWindowExecutionIntegrationTest#timeWindowFinalOrderByAndLimitAppliedBySemanticPipeline` |
| `query_model.time_window_result_stage_boundary` | signed-boundary | `timeWindow` requests accidentally mixed with ordinary query_model result-stage controls | Top-level `postAggregateCalculations` and `postSlice` are ordinary query_model result-stage controls, not timeWindow post-processing controls. Semantic query entrypoints reject them before execution or SQL preview; use timeWindow supported `calculatedFields` post projection or split into an ordinary result-stage query. | `TimeWindowExecutionIntegrationTest#timeWindowTopLevelResultStageRejectedBySemanticPipeline` |
| `query_model.time_window_having_boundary` | signed-boundary | `timeWindow` requests mixed with top-level `having` | Rejects top-level `having` before execution or SQL preview because it is ambiguous between base aggregate filtering and final result filtering; split the request or use ordinary query_model having outside timeWindow. | `TimeWindowExecutionIntegrationTest#timeWindowTopLevelHavingRejectedBySemanticPipeline` |
| `dsl_cte.time_window_bridge` | signed-constrained | DSL_CTE `aggregate -> window_derive` rolling sum and period-over-period lag/diff/growth templates | Maps signed DSL_CTE rolling and period-over-period templates into governed `timeWindow`. Output and final `orderBy` references to DSL_CTE window aliases are remapped to the actual timeWindow output aliases such as `__rolling_7d`, `__prior`, `__diff`, and `__ratio` before production `generateSql` reaches `QueryFacade`, including prior + diff + ratio output combinations. Ordinary DSL_CTE `postSlice` and post-aggregate `derive` stages are rejected in the same bridge so result-stage semantics cannot be silently treated as timeWindow post-processing. Top-level DSL_CTE `limit` is appended only when generated SQL has no trailing `LIMIT`; existing generated limits are not duplicated and invalid values are rejected. Neighboring but unsigned rolling / period-over-period shapes fail closed instead of being guessed into timeWindow. | `DslCteAcceptanceSampleTest#generateSqlOptInUsesDslBridgeForRollingWindow`, `DslCteAcceptanceSampleTest#generateSqlOptInUsesDslBridgeForLagPeriodOverPeriod`, `DslCteAcceptanceSampleTest#validationRemapsRollingAliasForFinalOrderBy`, `DslCteAcceptanceSampleTest#validationRemapsPeriodOverPeriodPriorAliasForOutputAndOrderBy`, `DslCteAcceptanceSampleTest#validationRemapsPeriodOverPeriodPriorDiffRatioOutputAndOrderBy`, `DslCteAcceptanceSampleTest#generateSqlOptInRemapsRollingFinalOrderByAlias`, `DslCteAcceptanceSampleTest#generateSqlOptInRemapsPeriodOverPeriodPriorOrderByAlias`, `DslCteAcceptanceSampleTest#generateSqlOptInRemapsPeriodOverPeriodPriorDiffRatioAliases`, `DslCteAcceptanceSampleTest#dslCteTopLevelLimitAppendsWhenBaseSqlHasNoLimit`, `DslCteAcceptanceSampleTest#dslCteTopLevelLimitDoesNotAppendWhenBaseSqlAlreadyLimited`, `DslCteAcceptanceSampleTest#dslCteTopLevelLimitRejectsInvalidValue`, `DslCteAcceptanceSampleTest#dslCteTimeWindowBridgeRejectsPostSlice`, `DslCteAcceptanceSampleTest#dslCteTimeWindowBridgeRejectsPostAggregateDerive`, `DslCteAcceptanceSampleTest#dslCtePeriodOverPeriodBridgeRejectsUnsignedNeighbors`, `DslCteAcceptanceSampleTest#dslCteRollingBridgeRejectsUnsignedNeighbors` |
| `query_model.pivot_base_grid` | signed | Rows, columns, metrics, subtotals, grand total, or cross-tab requests | Uses `payload.pivot`; mutually exclusive with top-level `columns` and `timeWindow`. | Pivot tests and query model descriptions |
| `query_model.pivot_axis_topn_having` | signed | Axis member TopN, axis sorting, or axis metric threshold | Uses `pivot.rows[*].orderBy`, `pivot.columns[*].orderBy`, `pivot.rows[*].limit`, `pivot.columns[*].limit`, and `pivot.rows[*].having`; top-level `orderBy` / `limit` are not Pivot axis controls and are rejected in Pivot mode. | `QueryModelDescriptionConsistencyTest`, `PivotIntegrationTest#testTopLevelOrderByAndLimitRejectedInPivotMode` |
| `query_model.pivot_result_stage_boundary` | signed-boundary | Pivot requests accidentally mixed with ordinary query_model result-stage controls | Top-level `postAggregateCalculations` and `postSlice` are ordinary query_model result-stage controls, not Pivot post-processing controls. Pivot mode rejects them before execution; use structured `pivot.metrics` or split into an ordinary result-stage query. | `PivotIntegrationTest#testTopLevelResultStageRejectedInPivotMode` |
| `query_model.pivot_c2_cascade_topn` | signed-constrained | Top N parents then Top N children under each surviving parent | Rows-axis exactly two levels, explicit `orderBy`, additive metrics, supported SQL dialect; unsupported shapes fail closed. | Pivot C2 implementation docs, `PivotCascadeGenerateValidationTest#testUnsupportedCascadeShapesRejectedAtPipelineBoundary`, `PivotCascadeGenerateValidationTest#testMysql57CascadeFailsClosedWithoutMemoryFallback` |
| `query_model.pivot_parent_share` | signed-constrained | Contribution to parent within a Pivot rows hierarchy | Uses structured `pivot.metrics.parentShare`; public `ROLLUP_TO` strings remain hidden/unsupported. Derived `parentShare` output is post-processing only and cannot be referenced by axis `having` or `orderBy`. | QueryModel schema descriptions, `PivotIntegrationTest#testDerivedMetricsRejectedAsAxisControls` |
| `query_model.pivot_baseline_ratio` | signed-constrained | Compare column cells to first/last baseline column | Uses structured `pivot.metrics.baselineRatio`; public `CELL_AT`, `AXIS_MEMBER`, and `AXIS_REF` strings remain hidden/unsupported. Derived `baselineRatio` output is post-processing only and cannot be referenced by axis `having` or `orderBy`. | QueryModel schema descriptions, `PivotIntegrationTest#testDerivedMetricsRejectedAsAxisControls` |
| `query_model.result_stage_cumulative_rank` | signed-constrained | Cumulative or rank formulas over selected aggregate aliases | Uses explicit `postAggregateCalculations`, governed calculatedFields sugar, or signed DSL_CTE derive formulas such as `cumulative_sum(alias)`, `cumulative_ratio_to_total(alias)`, and `rank_by(alias)`. The signed public shape is grand-total scope, DESC order, implicit frame only. Value-output kinds such as `rankByMeasure` and `cumulativeSum` use `format=value`; ratio-output kinds use `ratio` or `percent`. The runtime emits a `post_stage` CTE with `RANK()`, running sum, cumulative ratio, optional result-stage filter, and final alias order. DSL_CTE cumulative contribution with `postSlice` stays on this result-stage window SQL path and does not become a `timeWindow` bridge. Neighboring result-stage window shapes fail closed instead of being guessed into SQL. | `QueryRequestValidationStepTest#testPostAggregateUnsupportedRankingKindRejected`, `QueryRequestValidationStepTest#testPostAggregateUnsupportedDenseRankFormulaRejected`, `QueryRequestValidationStepTest#testPostAggregateUnsupportedFormulaOptionsRejected`, `QueryRequestValidationStepTest#testPostAggregateValueKindUnsupportedFormatRejected`, `QueryRequestValidationStepTest#testPostAggregateUnsignedRankingNeighborFormulasRejected`, `DslCteAcceptanceSampleTest#generateSqlOptInUsesResultStageWindowForCumulativeContribution`, `DslCteAcceptanceSampleTest#generateSqlOptInUsesDslBridgeForCumulativeRankDerivedStage`, `DslCteAcceptanceSampleTest#dslValidationRejectsUnsignedDenseRankDerivedFormula`, `DslCteAcceptanceSampleTest#dslValidationRejectsUnsignedResultStageFormulaOptions`, `DslCteAcceptanceSampleTest#resultStageWindowDefersUnsignedNeighbors`, `OdooModelLoadingTest#testExplicitCumulativeAndRankPostAggregateUsesOuterStage`, `OdooModelLoadingTest#testExplicitCumulativeAndRankPostAggregateMatchesFixtureBaseline`, `OdooModelLoadingTest#testDslCteCumulativeAndRankBridgeMatchesFixtureBaseline`, `OdooModelLoadingTest#testDslCteCumulativeAndRankExecuteModeMatchesFixtureBaseline`, `BUG-post-aggregate-alias-calculate-20260531.md`, promoted semantic gate |
| `query_model.cross_model_or_relation_composition` | routed | Multi-step relation composition that cannot be expressed as one query model request | Route to governed Memory Grid / CTE / compose-script flows without bypassing query model lifecycle, permissions, or physical column checks. | v3.8/v3.9 Memory Grid and runner docs |

## Unsigned or Deferred Variants

| Variant | Required behavior |
|---|---|
| SQL function field names such as `DATE_TRUNC(...)`, `YEAR(...)`, or `MONTH(...)` in public field references such as `slice`, `having`, `postSlice`, `groupBy`, or `orderBy` | Reject or rewrite to described date-grain fields; do not treat SQL functions as semantic field names. Legacy inline expressions in `columns` are not the signed date-grain route, but selected aliases from `columns` remain valid references. |
| Synthesized date-grain fields not returned by describe, such as appending `$month` to a plain date field | Reject or clarify; the field namespace is closed. |
| `pivot + timeWindow` | Reject or split into separate requests. |
| `columns + pivot` | Reject; use projection mode or Pivot mode. |
| `timeWindow` mixed with top-level `postAggregateCalculations` or `postSlice` | Reject before runtime execution or SQL preview; ordinary result-stage formulas and filters are not timeWindow post-processing controls. |
| `timeWindow` mixed with top-level `having` | Reject before runtime execution or SQL preview; `having` is ambiguous between base aggregate and final result filtering in the timeWindow derived route. |
| `timeWindow.calculatedFields` with sibling calculated-field alias dependencies | Reject before runtime execution or SQL preview; the signed timeWindow post-calc projection is a single derived stage over timeWindow output columns, not a multi-stage formula chain. |
| DSL_CTE timeWindow bridge mixed with `postSlice` or ordinary post-aggregate `derive` stages | Defer before DSL preview or execution; use the signed result-stage window bridge for cumulative/rank postSlice or an ordinary query_model result-stage route instead. |
| DSL_CTE rolling or period-over-period bridge neighbors that do not exactly match signed templates | Defer before DSL preview or execution. Unsupported variants include duplicate diff/ratio fields, wrong PoP formulas, explicit frames, descending window order, non-grouped partitions, unsupported rolling windows, rolling formulas that do not match the frame, multiple rolling outputs, and rolling input that is not the first aggregate stage. |
| Top-level `orderBy` / `limit` as Pivot TopN control | Reject or rewrite before runtime to axis-level `pivot.rows[*]` / `pivot.columns[*]` controls; raw Pivot runtime rejects top-level `orderBy` / `limit` before execution. |
| Pivot cascade without explicit orderBy on a limited level | Fail closed with the cascade order requirement. |
| Pivot cascade across both axes, column-axis cascade, three-level cascade, tree cascade, having-only cascade, or non-additive cascade totals | Fail closed; do not emulate with prompt-generated SQL. |
| `parentShare` or `baselineRatio` participating in axis `having`, `orderBy`, ambiguous window scope, tree mode, or cascade | Fail closed or simplify to ordinary Pivot metrics. Axis `having` / `orderBy` direct references to derived output aliases are rejected before execution; signed pre-page window scopes must be explicit. |
| Pivot mixed with top-level `postAggregateCalculations` or `postSlice` | Reject before execution; ordinary result-stage aliases and filters are not Pivot post-processing controls. |
| Public use of `ROLLUP_TO`, `CELL_AT`, `AXIS_MEMBER`, `AXIS_REF`, arbitrary MDX set algebra, or coordinate functions | Reject; only structured metric objects are signed. |
| Free-form window, cumulative, ratio, or rank expressions outside governed result-stage formula shapes | Defer until a typed recipe and negative gates exist. In particular, dense-rank/row-number/percent-rank style calculatedFields, DSL_CTE derive formulas, and post-aggregate kinds are not signed by the public query_model postAggregate contract. |
| Result-stage formula options beyond grand-total DESC implicit-frame semantics | Reject before compilation. `ASC` ordering, partitioned contribution, explicit window frames, tie-breakers, and extra named arguments are unsigned until represented as typed recipe fields with fixture parity and negative gates. |
| Explicit `postAggregateCalculations` value-output kinds with ratio/percent formats | Reject before SQL generation. `rankByMeasure` and `cumulativeSum` produce values, not ratios; use `format=value` or omit format. |
| DSL_CTE result-stage cumulative/rank bridge neighbors outside the signed `rank()` plus cumulative-share template | Defer before DSL preview or execution. Unsupported variants include partitioned windows, explicit frames, non-DESC ordering, multiple aggregate metrics, missing grouping tie-breakers, unsigned rank functions, and cumulative formulas that do not reference the signed metric shape. |
| Raw CTE or compose-script intended to bypass model permissions or lifecycle | Reject; model-backed access must enter through `query_model` first. |

## Runtime Evidence

| Evidence | Meaning |
|---|---|
| `OdooModelLoadingTest#testExplicitCumulativeAndRankPostAggregateUsesOuterStage` | Explicit `postAggregateCalculations` for `rankByMeasure`, `cumulativeSum`, and `cumulativeRatioToTotal` generate a `post_stage` CTE with result-stage filter and final alias ordering. |
| `OdooModelLoadingTest#testExplicitCumulativeAndRankPostAggregateMatchesFixtureBaseline` | Executed `postAggregateCalculations` rows match a hand-built `sale_order` + `crm_team` fixture baseline for team sales, SQL `RANK()`, cumulative sum, and cumulative ratio. |
| `OdooModelLoadingTest#testDslCteCumulativeAndRankBridgeMatchesFixtureBaseline` | Signed DSL_CTE ordinary derive formulas for rank and cumulative contribution assemble structured CTE SQL, execute the `post_stage` result filter, and match the Odoo `sale_order` + `crm_team` baseline. |
| `OdooModelLoadingTest#testDslCteCumulativeAndRankExecuteModeMatchesFixtureBaseline` | Production `queryModel(..., "execute", ...)` uses assembled structured CTE SQL and assembled params for the same signed DSL_CTE request, returning rows that match the Odoo fixture baseline. |
| `OdooModelLoadingTest#testSaleOrderGroupedTopNOrderLimitMatchesFixtureBaseline` | Production `queryModel(..., "execute", ...)` runs a grouped `sale_order` + `crm_team` aggregate table, orders by the selected aggregate alias, applies top-level `limit`, and matches the hand-built Odoo fixture TopN baseline. |
| `OdooInvalidFieldRecoveryTest#testSynthesizedDateOrderQuarterIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects a synthesized `dateOrder$quarter` field with `INVALID_QUERY_FIELD` before SQL generation, proving the date-grain namespace is closed to fields not returned by describe. |
| `OdooInvalidFieldRecoveryTest#testSqlFunctionGroupByFieldIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects direct `YEAR(dateOrder)` usage in `groupBy` with `INVALID_QUERY_FIELD` before SQL generation; callers must use described date-grain fields or selected aliases. |
| `OdooInvalidFieldRecoveryTest#testSqlFunctionOrderByFieldIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects direct `YEAR(dateOrder)` usage in `orderBy` with `INVALID_QUERY_FIELD` before SQL generation; selected aliases and governed calculated fields remain the supported ordering route. |
| `OdooInvalidFieldRecoveryTest#testSqlFunctionSliceFieldIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects direct `YEAR(dateOrder)` usage in `slice` with `INVALID_QUERY_FIELD` before SQL generation; filters must use described fields or selected aliases. |
| `OdooInvalidFieldRecoveryTest#testSqlFunctionHavingFieldIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects direct `YEAR(dateOrder)` usage in `having` with `INVALID_QUERY_FIELD` before SQL generation; aggregate filters must use described fields, aggregate aliases, or governed result-stage routes. |
| `OdooInvalidFieldRecoveryTest#testSqlFunctionPostSliceFieldIsRejectedBeforeSql` | Public `queryModel(..., "execute", ...)` rejects direct `YEAR(dateOrder)` usage in `postSlice` with `INVALID_QUERY_FIELD` before SQL generation; result-stage filters must use selected aliases or governed calculated fields. |
| `OdooModelLoadingTest#testSaleOrderInlineYearAliasGroupByOrderByExecutes` | Public `queryModel(..., "execute", ...)` preserves the legacy `columns` inline-expression alias route: `YEAR(dateOrder) as orderYear` may be selected first and then referenced by `groupBy` / `orderBy`. |
| `PivotIntegrationTest#testMutuallyExclusiveValidation` | Pivot runtime rejects `pivot + columns` and `pivot + timeWindow` before execution, preserving the one-route contract between ordinary query projection, `timeWindow`, and Pivot. |
| `PivotIntegrationTest#testTopLevelOrderByAndLimitRejectedInPivotMode` | Pivot runtime rejects top-level `orderBy` and `limit` before execution and points callers to axis-level `pivot.rows[*]` / `pivot.columns[*]` sorting and TopN controls. |
| `PivotIntegrationTest#testDerivedMetricsRejectedAsAxisControls` | Pivot runtime rejects axis `orderBy` / `having` references to derived `parentShare` / `baselineRatio` output aliases before execution, because these metrics are post-processing outputs rather than axis-domain selection inputs. |
| `PivotCascadeGenerateValidationTest#testUnsupportedCascadeShapesRejectedAtPipelineBoundary` | Pivot pipeline pre-validation rejects unsupported cascade neighbors before QueryModel loading or QueryFacade execution: missing limited-level `orderBy`, column-axis cascade, rows cascade plus column TopN, three-level cascade, and multi-level having-only cascade. |
| `PivotCascadeGenerateValidationTest#testMysql57CascadeFailsClosedWithoutMemoryFallback` | Pivot cascade SQL pushdown fails closed on unsupported conservative MySQL dialects before final SQL execution instead of silently falling back to memory emulation. |
| `PivotIntegrationTest#testTopLevelResultStageRejectedInPivotMode` | Pivot runtime rejects top-level `postAggregateCalculations` and `postSlice` before execution, keeping ordinary result-stage controls separate from Pivot metrics and axis filters. |
| `TimeWindowExecutionIntegrationTest#timeWindowPostCalcDependencyRejectedBySemanticPipeline` | Semantic query entrypoints reject sibling alias dependencies inside `timeWindow.calculatedFields` before SQL preview with `TIMEWINDOW_POST_CALCULATED_FIELD_DEPENDENCY_UNSUPPORTED`, preventing dialect-dependent same-select alias resolution failures. |
| `TimeWindowExecutionIntegrationTest#timeWindowFinalOrderByAndLimitAppliedBySemanticPipeline` | Semantic query entrypoints compile and execute top-level `orderBy` / `limit` as a final outer timeWindow result stage; generated SQL includes `ORDER BY` and `LIMIT` over a timeWindow output alias. |
| `TimeWindowExecutionIntegrationTest#timeWindowTopLevelResultStageRejectedBySemanticPipeline` | Semantic query entrypoints reject `timeWindow` requests mixed with top-level `postAggregateCalculations` or `postSlice` before execution or SQL preview, keeping ordinary result-stage controls separate from the timeWindow derived result stage. |
| `TimeWindowExecutionIntegrationTest#timeWindowTopLevelHavingRejectedBySemanticPipeline` | Semantic query entrypoints reject `timeWindow + having` before SQL preview with `TIME_WINDOW_HAVING_UNSUPPORTED`, preventing silent ambiguity between base aggregate and final result filters. |
| `DslCteAcceptanceSampleTest#validationRemapsRollingAliasForFinalOrderBy` | DSL_CTE bridge validation maps final `orderBy` over a rolling derived alias to the actual timeWindow output alias before handing the request to the timeWindow route. |
| `DslCteAcceptanceSampleTest#validationRemapsPeriodOverPeriodPriorAliasForOutputAndOrderBy` | DSL_CTE bridge validation maps period-over-period prior alias output and final `orderBy` to the actual `__prior` timeWindow output alias. |
| `DslCteAcceptanceSampleTest#validationRemapsPeriodOverPeriodPriorDiffRatioOutputAndOrderBy` | DSL_CTE bridge validation maps period-over-period prior, difference, and ratio outputs together to `__prior`, `__diff`, and `__ratio`, and remaps final `orderBy` over the difference alias. |
| `DslCteAcceptanceSampleTest#generateSqlOptInRemapsRollingFinalOrderByAlias` | Production `generateSql` opt-in path hands `QueryFacade` a rolling timeWindow bridge request whose final `orderBy` field has already been remapped from the DSL_CTE alias to `salesAmount__rolling_7d`. |
| `DslCteAcceptanceSampleTest#generateSqlOptInRemapsPeriodOverPeriodPriorOrderByAlias` | Production `generateSql` opt-in path hands `QueryFacade` a period-over-period timeWindow bridge request whose prior output and final `orderBy` field have already been remapped to `balanceAmount__prior`. |
| `DslCteAcceptanceSampleTest#generateSqlOptInRemapsPeriodOverPeriodPriorDiffRatioAliases` | Production `generateSql` opt-in path hands `QueryFacade` a period-over-period timeWindow bridge request whose prior, difference, and ratio output aliases and final `orderBy` field have already been remapped. |
| `DslCteAcceptanceSampleTest#dslCteTopLevelLimitAppendsWhenBaseSqlHasNoLimit` | Top-level DSL_CTE `limit` appends a final `LIMIT ?` and parameter only when generated SQL has no trailing limit. |
| `DslCteAcceptanceSampleTest#dslCteTopLevelLimitDoesNotAppendWhenBaseSqlAlreadyLimited` | Top-level DSL_CTE `limit` does not duplicate an existing trailing generated `LIMIT ?`, preserving the original generated SQL and params. |
| `DslCteAcceptanceSampleTest#dslCteTopLevelLimitRejectsInvalidValue` | Invalid top-level DSL_CTE `limit` values are rejected before returning SQL. |
| `DslCteAcceptanceSampleTest#dslCteTimeWindowBridgeRejectsPostSlice` | DSL_CTE bridge validation defers a signed rolling timeWindow bridge when a `postSlice` stage is appended, keeping timeWindow and result-stage filtering routes separate. |
| `DslCteAcceptanceSampleTest#dslCteTimeWindowBridgeRejectsPostAggregateDerive` | DSL_CTE bridge validation defers a signed rolling timeWindow bridge when an ordinary result-stage `derive` formula is mixed into the same bridge, keeping post-aggregate calculations out of timeWindow post-processing. |
| `DslCteAcceptanceSampleTest#dslCtePeriodOverPeriodBridgeRejectsUnsignedNeighbors` | DSL_CTE bridge validation defers neighboring period-over-period shapes such as duplicate diff/ratio outputs, wrong formulas, explicit frames, descending order, and non-grouped partitions. |
| `DslCteAcceptanceSampleTest#dslCteRollingBridgeRejectsUnsignedNeighbors` | DSL_CTE bridge validation defers neighboring rolling shapes such as unsupported windows, formula/frame mismatches, unsupported aggregators, multiple rolling outputs, partitions, descending order, and non-first aggregate inputs. |
| `OdooModelLoadingTest#testCalculatedFieldsCumulativeAndRankNormalizeToPostAggregate` | Governed calculatedFields sugar normalizes to the same post-aggregate contract and removes the sugar fields from ordinary calculated field processing. |
| `DslCteAcceptanceSampleTest#generateSqlOptInUsesResultStageWindowForCumulativeContribution` | DSL_CTE cumulative contribution with signed `postSlice` generates result-stage window SQL outside the base `QueryFacade` aggregate request and does not carry a `timeWindow` extension. |
| `DslCteAcceptanceSampleTest#generateSqlOptInUsesDslBridgeForCumulativeRankDerivedStage` | Signed DSL_CTE ordinary derive formulas for rank, cumulative sum, and cumulative ratio bridge to `postAggregateCalculations` instead of remaining prompt-only formula text. |
| `DslCteAcceptanceSampleTest#dslValidationRejectsUnsignedDenseRankDerivedFormula` | DSL_CTE validation rejects unsigned `dense_rank()` result-stage derived formulas before compilation. |
| `DslCteAcceptanceSampleTest#dslValidationRejectsUnsignedResultStageFormulaOptions` | DSL_CTE validation rejects signed-looking result-stage formulas that request unsigned `ASC`, partition, explicit-frame, or extra-argument semantics. |
| `QueryRequestValidationStepTest#testPostAggregateUnsupportedRankingKindRejected` | Unsigned ranking kinds such as dense-rank style post-aggregate calculations are rejected before SQL generation. |
| `QueryRequestValidationStepTest#testPostAggregateUnsupportedDenseRankFormulaRejected` | Unsigned `dense_rank()` calculatedFields formulas are rejected before SQL generation. |
| `QueryRequestValidationStepTest#testPostAggregateUnsupportedFormulaOptionsRejected` | Signed-looking calculatedFields formula options outside the governed shape are rejected before SQL generation. |
| `QueryRequestValidationStepTest#testPostAggregateValueKindUnsupportedFormatRejected` | Explicit `postAggregateCalculations` value-output kinds reject ratio/percent formats before SQL generation. |
| `QueryRequestValidationStepTest#testPostAggregateUnsignedRankingNeighborFormulasRejected` | CalculatedFields rank/window neighbors such as `row_number`, `percent_rank`, tie-breaker arguments, and filter arguments are rejected before SQL generation. |
| `DslCteAcceptanceSampleTest#resultStageWindowDefersUnsignedNeighbors` | DSL_CTE result-stage cumulative/rank bridge defers neighboring unsigned shapes such as partition, explicit frame, ASC order, multiple metrics, missing grouping tie-breakers, unsupported rank functions, and wrong cumulative formulas. |
| `BUG-post-aggregate-alias-calculate-20260531.md` | v3.9 root record for alias ratio, cumulative contribution, and rank-by-measure fixes and regression evidence. |

## Required Touchpoints For New Query Recipes

| Touchpoint | Required update |
|---|---|
| Query model schema descriptions | Update `query_model_v3.md`, `query_model_v3_basic.md`, and `query_model_v3_no_vector.md` together. |
| Description consistency test | Add planner-visible snippets to `QueryModelDescriptionConsistencyTest`. |
| Fixture or replay sample | Add positive evidence for the signed recipe. |
| Negative gate | Add close-neighbor failures for underspecified or unsupported variants. |
| Runtime validator or mapper | Add a signed branch or explicit fail-closed branch. |
| Version docs | Update this index and the owning v3.9 workitem. |

## Extension Rule

Do not promote a new query or analysis capability through prompt text alone. A capability is signed only when its fields, lifecycle route, accepted shape, negative variants, and evidence are recorded together.
