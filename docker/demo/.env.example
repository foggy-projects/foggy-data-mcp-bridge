# ============================================
# Foggy MCP Demo - 环境变量配置
# ============================================
# 使用方法:
#   1. 复制此文件: cp .env.example .env
#   2. 编辑 .env 填入真实配置
#   3. 启动服务: docker-compose up -d
# ============================================

# ------------------------------------------
# AI 配置 (必填)
# ------------------------------------------
# OpenAI API Key (必填)
OPENAI_API_KEY=sk-your-api-key-here

# AI 服务地址
# OpenAI: https://api.openai.com
# 阿里云百炼: https://dashscope.aliyuncs.com/compatible-mode/v1
# Ollama 本地: http://host.docker.internal:11434/v1
OPENAI_BASE_URL=https://api.openai.com

# 模型名称
# OpenAI: gpt-4o-mini, gpt-4o, gpt-4-turbo
# 阿里云: qwen-plus, qwen-turbo, qwen-max
# Ollama: qwen2:7b, llama3:8b
OPENAI_MODEL=gpt-4o-mini

# ------------------------------------------
# MCP 服务配置
# ------------------------------------------
# MCP 服务端口
MCP_PORT=7108
MCP_M2_PORT=7109

# 服务层 (m1=智能Agent, m2=数据分析师, both=两者)
MCP_LAYER=both

# 可用的查询模型列表 (逗号分隔)
# 演示环境默认包含电商数据模型
# 添加自定义模型后，需要将模型名追加到此列表
MCP_MODEL_LIST=FactSalesQueryModel,FactOrderQueryModel,FactPaymentQueryModel,FactReturnQueryModel,FactInventorySnapshotQueryModel

# 日志级别 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ------------------------------------------
# 数据库配置 (演示数据库)
# ------------------------------------------
MYSQL_PORT=13306
MYSQL_DATABASE=foggy_test
MYSQL_USER=foggy
MYSQL_PASSWORD=foggy_test_123
MYSQL_ROOT_PASSWORD=foggy_root_123

# ------------------------------------------
# 外部自定义模型配置
# ------------------------------------------
# 是否启用外部模型 (设为 true 后需重启)
EXTERNAL_MODELS_ENABLED=false

# 外部模型目录路径 (相对于 docker-compose.yml)
EXTERNAL_MODELS_PATH=./custom-models

# ------------------------------------------
# 自定义数据源配置 (供外部模型使用)
# ------------------------------------------
# 在外部模型的 datasource.fsscript 中使用:
#   dataSourceFactory.env('CUSTOM_DB_URL')
#
# 示例:
# CUSTOM_DB_URL=jdbc:mysql://host.docker.internal:3306/mydb
# CUSTOM_DB_USER=myuser
# CUSTOM_DB_PASS=mypassword
CUSTOM_DB_URL=
CUSTOM_DB_USER=
CUSTOM_DB_PASS=

# ------------------------------------------
# 可选: 数据库管理工具端口
# ------------------------------------------
ADMINER_PORT=18080

# ------------------------------------------
# 可选: JVM 配置
# ------------------------------------------
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC

# ------------------------------------------
# 版本
# ------------------------------------------
FOGGY_VERSION=8.0.0-beta
