# ============================================================
# 极简模式（Lite Profile）
# 无需 MongoDB，仅保留核心 MCP + JDBC 查询能力
#
# 启动方式：
#   java -jar foggy-mcp-launcher.jar --spring.profiles.active=lite
#   或 SPRING_PROFILES_ACTIVE=lite
#
# 数据源：使用可重建的本地 SQLite 文件作为启动数据源
# 实际业务数据源通过 DataSource API 动态配置。
# 不使用 in-memory SQLite，避免长时间 matrix 运行中连接池回收导致 demo schema 消失。
# ============================================================

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoRepositoriesAutoConfiguration

  datasource:
    url: "jdbc:sqlite:${MCP_LITE_SQLITE_PATH:${java.io.tmpdir}/foggy_mcp_lite.db}"
    driver-class-name: org.sqlite.JDBC
    hikari:
      pool-name: FoggyMcpLiteSQLiteHikariCP
      minimum-idle: 1
      maximum-pool-size: 1
      idle-timeout: 0
      max-lifetime: 0
      connection-test-query: SELECT 1

  sql:
    init:
      mode: always
      schema-locations: classpath:/db/lite-demo-schema.sql
      data-locations: classpath:/db/lite-demo-data.sql

foggy:
  compose:
    dialect: sqlite
  # 关闭 Data Viewer（强依赖 MongoDB）
  data-viewer:
    enabled: false

  mcp:
    semantic:
      model-list:
        - FactOrderQueryModel
        - CrmLead
        - CustomerOrderLifecycleQueryModel
        - ServiceTicketQueryModel

    # 关闭审计日志（需要 MongoDB）
    audit:
      enabled: false

    # 禁用依赖 Data Viewer 的 MCP 工具
    tools:
      - name: "dataset.open_in_viewer"
        enabled: false
