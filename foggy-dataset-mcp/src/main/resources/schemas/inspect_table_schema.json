{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Inspect database table structure for TM generation",
  "properties": {
    "table_name": {
      "type": "string",
      "description": "Name of the table to inspect"
    },
    "schema": {
      "type": "string",
      "description": "Database schema name (optional, uses default if not specified)"
    },
    "include_indexes": {
      "type": "boolean",
      "description": "Whether to include index information (default: false)",
      "default": false
    },
    "include_foreign_keys": {
      "type": "boolean",
      "description": "Whether to include foreign key relationships (default: true)",
      "default": true
    }
  },
  "required": ["table_name"]
}
