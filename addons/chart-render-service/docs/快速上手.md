# 图形渲染服务 - 快速上手

## 5分钟上手指南

### 1. 服务信息
- **地址**: `http://localhost:3001`
- **认证**: `Authorization: default-render-token`

### 2. 健康检查
```bash
curl http://localhost:3001/healthz
```

### 3. 第一个图表

**复制粘贴即可运行**：

```bash
curl -X POST http://localhost:3001/render/native \
  -H "Authorization: default-render-token" \
  -H "Content-Type: application/json" \
  -d '{
    "engine": "echarts",
    "engine_spec": {
      "title": {"text": "我的第一个图表"},
      "xAxis": {
        "type": "category",
        "data": ["周一", "周二", "周三", "周四", "周五"]
      },
      "yAxis": {"type": "value"},
      "series": [{
        "data": [120, 200, 150, 80, 70],
        "type": "bar"
      }]
    },
    "image": {
      "format": "png",
      "width": 800,
      "height": 600
    }
  }'
```

### 4. 保存图片（JavaScript）

```javascript
const response = await fetch('http://localhost:3001/render/native', {
  method: 'POST',
  headers: {
    'Authorization': 'default-render-token',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    engine: 'echarts',
    engine_spec: {
      title: { text: '销售数据' },
      xAxis: { type: 'category', data: ['产品A', '产品B', '产品C'] },
      yAxis: { type: 'value' },
      series: [{ data: [100, 200, 150], type: 'bar' }]
    },
    image: { format: 'png', width: 600, height: 400 }
  })
});

const result = await response.json();
if (result.success) {
  // 图片的Base64数据在 result.image 中
  console.log('渲染成功！');
}
```

## 常用图表类型

### 柱状图
```json
{
  "engine_spec": {
    "xAxis": {"type": "category", "data": ["A", "B", "C"]},
    "yAxis": {"type": "value"},
    "series": [{"data": [120, 200, 150], "type": "bar"}]
  }
}
```

### 折线图
```json
{
  "engine_spec": {
    "xAxis": {"type": "category", "data": ["1月", "2月", "3月"]},
    "yAxis": {"type": "value"},
    "series": [{"data": [820, 932, 901], "type": "line"}]
  }
}
```

### 饼图
```json
{
  "engine_spec": {
    "series": [{
      "type": "pie",
      "radius": "60%",
      "data": [
        {"name": "类别A", "value": 335},
        {"name": "类别B", "value": 310},
        {"name": "类别C", "value": 234}
      ]
    }]
  }
}
```

## 统一语义（更简单）

```json
{
  "unified": {
    "type": "bar",
    "title": "销售数据",
    "xField": "month",
    "yField": "sales"
  },
  "data": [
    {"month": "1月", "sales": 1000},
    {"month": "2月", "sales": 1200}
  ]
}
```

## 错误排查

| 问题 | 解决方案 |
|------|----------|
| 401错误 | 检查Authorization头 |
| 渲染失败 | 检查ECharts配置语法 |
| 超时 | 减少数据量或降低分辨率 |

## 下一步

- 📖 阅读[完整使用指南](./用户使用指南.md)
- 🔧 查看项目[README.md](../README.md)
- 🎯 集成到你的应用中

---

**提示**: 所有返回的图片都是Base64编码，可以直接保存或嵌入HTML。