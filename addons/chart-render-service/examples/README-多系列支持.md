      12
## 功能概述

chart-render-service现在支持通过`seriesField`参数在统一语义渲染中生成多系列图表，无需使用复杂的原生ECharts配置。

## 使用方法

### 1. 单系列数据 (原有功能)
```json
{
  "unified": {
    "type": "line",
    "xField": "month",
    "yField": "sales"
  },
  "data": [
    {"month": "1月", "sales": 1000},
    {"month": "2月", "sales": 1200}
  ]
}
```
→ 生成1条折线

### 2. 多系列数据 (新功能)
```json
{
  "unified": {
    "type": "line",
    "xField": "month",
    "yField": "sales",
    "seriesField": "product"  // 👈 关键参数
  },
  "data": [
    {"month": "1月", "product": "iPhone", "sales": 1000},
    {"month": "1月", "product": "iPad", "sales": 800},
    {"month": "2月", "product": "iPhone", "sales": 1200},
    {"month": "2月", "product": "iPad", "sales": 900}
  ]
}
```
→ 生成2条折线 (iPhone一条，iPad一条)

## 支持的图表类型

- ✅ **line** - 多系列折线图
- ✅ **bar/column** - 多系列柱状图
- ✅ **area** - 多系列面积图
- ❌ pie/doughnut - 饼图本身就是多系列的

## 核心参数

| 参数 | 说明 | 示例 |
|-----|-----|-----|
| `seriesField` | 指定哪个字段用于分组系列 | "product", "category", "type" |
| `xField` | X轴数据字段 | "month", "date", "time" |
| `yField` | Y轴数值字段 | "sales", "amount", "count" |

## 数据格式要求

1. **扁平化数据**: 每行一个数据点
2. **包含分组字段**: seriesField指定的字段必须存在
3. **完整性**: 建议每个系列在各个X轴值都有数据点

```json
// ✅ 正确格式
[
  {"time": "1月", "product": "A", "value": 100},
  {"time": "1月", "product": "B", "value": 200},
  {"time": "2月", "product": "A", "value": 150},
  {"time": "2月", "product": "B", "value": 250}
]

// ❌ 错误格式 - 嵌套结构
{
  "A": [{"time": "1月", "value": 100}],
  "B": [{"time": "1月", "value": 200}]
}
```

## 自动功能

### 1. 智能颜色分配
系统自动为每个系列分配不同颜色：
- 最多支持20个系列
- 使用预定义的配色方案
- 超出系列数量会循环使用颜色

### 2. 数据处理
- 自动按X轴值排序
- 处理缺失数据点 (显示为空)
- 支持TopN限制

### 3. 图例和工具提示
- 自动生成图例显示所有系列
- 工具提示显示多系列对比数据

## 性能限制

- **最大系列数**: 20个 (超出会选择数值最大的前20个)
- **数据点建议**: 每个系列不超过1000个点
- **自动优化**: 大数据集会自动聚合处理

## 完整示例

使用你提供的数据:

```bash
curl -X POST http://localhost:3001/render/unified \
  -H "Authorization: default-render-token" \
  -H "Content-Type: application/json" \
  -d '{
    "unified": {
      "type": "line",
      "title": "商品销售趋势对比",
      "xField": "month",
      "yField": "amount",
      "seriesField": "product",
      "smooth": true,
      "showLegend": true
    },
    "data": [
      {"month": "2024-01", "product": "iPhone", "amount": 85000},
      {"month": "2024-02", "product": "iPhone", "amount": 92000},
      // ... 更多数据
    ],
    "image": {"format": "png", "width": 1200, "height": 600}
  }'
```

## 测试脚本

运行测试脚本验证功能:
```bash
./examples/test-multi-series.sh
```

## 与原生ECharts对比

| 特性 | 统一语义 + seriesField | 原生ECharts |
|-----|-----|-----|
| 配置复杂度 | 🟢 简单 | 🔴 复杂 |
| 数据格式 | 🟢 扁平化 | 🔴 需要预处理 |
| 自动功能 | 🟢 颜色、图例、格式化 | 🔴 手动配置 |
| 定制能力 | 🔴 有限 | 🟢 无限制 |
| 学习成本 | 🟢 低 | 🔴 高 |

**建议**:
- 简单多系列图表 → 使用统一语义 + seriesField
- 复杂定制需求 → 使用原生ECharts