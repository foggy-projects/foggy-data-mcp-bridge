package com.foggyframework.analytics.console.model;

/** Product conversation mode; QUESTION deliberately has no Analytics asset dependency. */
public enum AnalyticsConsoleConversationMode {
    QUESTION,
    DESIGN
}
