package com.foggyframework.analytics.console.security;

/** Product roles owned by Analytics Console, independent from Runtime and FAP roles. */
public enum AnalyticsConsoleRole {
    ADMIN,
    DESIGNER,
    VIEWER
}
