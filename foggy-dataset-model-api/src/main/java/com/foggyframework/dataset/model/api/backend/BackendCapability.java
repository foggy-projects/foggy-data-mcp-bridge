package com.foggyframework.dataset.model.api.backend;

/** Independently discoverable backend capabilities. */
public enum BackendCapability {
    MODEL_LOAD,
    QUERY,
    ATOMIC_REFRESH,
    CACHE_INVALIDATION
}
