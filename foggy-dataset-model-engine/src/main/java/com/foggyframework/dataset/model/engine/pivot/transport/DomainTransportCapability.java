package com.foggyframework.dataset.model.engine.pivot.transport;

/**
 * Empty wrapper/capability marker interface or class if needed in future.
 */
public class DomainTransportCapability {
    public static boolean isSupportedByDialect(String productName) {
        // Just a marker class for now
        return true;
    }
}
