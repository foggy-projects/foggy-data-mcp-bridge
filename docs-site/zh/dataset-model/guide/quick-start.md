# 快速开始

## 安装

在 `pom.xml` 中添加依赖：

```xml
<dependency>
    <groupId>com.foggysource</groupId>
    <artifactId>foggy-dataset-model</artifactId>
    <version>8.0.0-beta</version>
</dependency>
```

## 基本用法

即将推出...
