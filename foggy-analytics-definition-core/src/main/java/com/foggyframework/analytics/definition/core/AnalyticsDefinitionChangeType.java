package com.foggyframework.analytics.definition.core;

/** Stable operation applied to one logical definition between bundle revisions. */
public enum AnalyticsDefinitionChangeType {
    ADDED,
    MODIFIED,
    REMOVED
}
